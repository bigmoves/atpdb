{
  "phases": {
    "build": {
      "cmd": ["cargo", "build", "--release"]
    }
  },
  "start": {
    "cmd": ["./target/release/atpdb", "serve", "--port=$PORT", "--connect"]
  },
  "variables": {
    "CARGO_HOME": "/nix/store/.cargo",
    "RUST_LOG": "info",
    "RUST_VERSION": "1.93.0"
  }
}